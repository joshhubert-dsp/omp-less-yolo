#!/usr/bin/env bash
#MISE description="Smoke test that python3 is available in the container"
set -euo pipefail

DOCKER_CMD="${PI_CONTAINER_RUNTIME:-docker}"
"${DOCKER_CMD}" run --rm "${OMP_IMAGE}" bash -c 'python3 --version'
